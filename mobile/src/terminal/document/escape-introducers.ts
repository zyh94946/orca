/** The escape byte every terminal report the document writes is prefixed with. */
export const ESC = '\x1b'

/** The C1 form of the control sequence introducer, which a host may send instead of `ESC [`. */
export const C1_CSI = '\x9b'
